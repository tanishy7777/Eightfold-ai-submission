"""Multi-source candidate data transformer.

Turns messy candidate inputs from many sources into one canonical profile
with provenance and confidence, then projects that profile through a
runtime config. See pipeline.py for the readable end-to-end flow.
"""
